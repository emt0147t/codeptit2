services:
  - type: web
    name: codeptitclone
    env: docker
    plan: free
    dockerCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: SECRET_KEY
        generateValue: true
